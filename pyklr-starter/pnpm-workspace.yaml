packages:
  - 'apps/*'
  - 'packages/*'

# Force lightningcss 1.30.1 globally — 1.33.0 has a deserialization bug with
# Tailwind v4 / react-native-css ("expected an object-like struct named Specifier").
overrides:
  lightningcss: 1.30.1
